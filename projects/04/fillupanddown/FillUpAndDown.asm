// This file is inspired on the work done in www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Fábio Dias, IST.
// File name: projects/04/FillUpAndDown.asm

// When a key is pressed (any key), the program blackens the screen,
// i.e. writes "black" in every pixel;
// the screen should remain fully black as long as the key is pressed. 
// When no key is pressed, the program clears the screen, i.e. writes
// "white" in every pixel;
// the screen should remain fully clear as long as no key is pressed.
// If you press the "up" key only the upper half changes to black while
// the button remains white and vice versa with the down key.
// Runs an infinite loop that listens to the keyboard input.

// Put your code here.
