// Program: NumbersToMemory.asm
// Description: Press a key of a number at some moment and stores that number at RAM[0] 
//      until other key is pressed (don't validate if input is a number)
// Usage: Press a key of a number and check the value at RAM[0]
