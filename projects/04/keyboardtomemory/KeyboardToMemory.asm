// Program: KeyboardToMemory.asm
// Description: Press a key at some moment and stores the code of the key at RAM[0] until other key is pressed
// Usage: Press a key and check the value at RAM[0]

// When a key is pressed, it's value is saved at R0 (RAM[0]).

// Put your code here.